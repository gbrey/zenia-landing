# Zenia — landing

PWA de presentación de Zenia. Estáticos en `public/`, deploy en Cloudflare Pages.

## Local

```bash
npm install
npm run dev
```

## Deploy

Cada push a `main` dispara el build de Cloudflare Pages (`public/`).
